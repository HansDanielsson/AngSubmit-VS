# AngSubmit-VS
Skapa 2 kataloger med 6 filer enligt:
mkdir Angular, som innehåller angular.js och (angular.min.js + *.map)
mkdir Bootstrap, som innehåller bootstrap.css och (bootstrap.min.css + *.map)